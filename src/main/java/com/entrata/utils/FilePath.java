package com.entrata.utils;

public class FilePath {
	/**
	 * We can utilize this class for reading all config files and also can manage env specific files
	 */

	public static String configFilePath = System.getProperty("user.dir") + "/src/test/resources/TestData/config.properties";
}
